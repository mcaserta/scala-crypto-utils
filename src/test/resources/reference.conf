keystores: {
  keystore1: {location: "keystore.jks", password: "password"},
  keystore2: {location: "keystore.jks", password: "password"}
}

publicKeys: {
  publicKey1: {alias: "test", keystore: "keystore1"},
  publicKey2: {alias: "test", keystore: "keystore2"}
}

privateKeys: {
  privateKey1: {
    alias: "test",
    password: "password",
    keystore: "keystore1"
  },
  privateKey2: {
    alias: "test",
    password: "password",
    keystore: "keystore2"
  }
}

